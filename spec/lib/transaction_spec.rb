require "spec_helper"
require "transaction"

describe Transaction do
  it "has an unknown initial type"
  it "has no initial amount"
  it "has no timestamp"
  it "has no description"
  it "can set amount"
  it "can set a type"
  it "can set a timestamp"

end